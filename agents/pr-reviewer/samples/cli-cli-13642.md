> **Sample output** — generated by `claude-review` on a real PR.
> PR: [cli/cli#13642](https://github.com/cli/cli/pull/13642)
> Command: `./claude-review --pr cli/cli#13642`

---

## 🔍 PR Review: Make filtering by bot authors more discoverable

### Summary
This PR improves discoverability of the existing `--app` flag for filtering by GitHub App (bot) authors across `issue list`, `pr list`, `search issues`, and `search prs`. It does so by appending a hint to each `--author` flag's help text, adding `--app dependabot` usage examples, and expanding the `gh` skill docs with guidance on bot-author filtering and correct search-qualifier quoting. No behavioral code changes — only help strings, examples, and Markdown docs.

### ⚠️ Risks
- **Doc-only change, low blast radius.** No logic, flags, or option parsing are modified, so functional regression risk is essentially nil.
- **Unverifiable factual claims in `SKILL.md`.** The docs assert specific behaviors that aren't visible in this diff: that `--app` "expands to `author:app/<slug>`", that `--author dependabot` "matches nothing", and that `gh search issues "repo:cli/cli is:open"` is parsed as `repo:"cli/cli is:open"` and fails with `Invalid search query`. If any of these is slightly off (e.g. exact error text, or the precise expansion format), the docs will mislead. These should be confirmed against the actual implementation/CLI behavior.
- **`--author "dependabot[bot]"` guidance.** The skill suggests this as an alternative. In a shell, the `[bot]` brackets and `@me`-style values can be glob-sensitive; the quoting shown is correct, but it's worth confirming this form actually works against the search index (apps vs. user logins).

### 💡 Suggestions
- **Inconsistent example coverage.** `issue list` and `pr list` gained `--app` usage examples, but `search issues` and `search prs` only got the help-text tweak. For parity (and since the skill explicitly says `--app` works on `search prs|issues`), consider adding a short `--app` example to the two search commands' `Example` blocks too.
- **Wording symmetry.** The `pr list` example comment ("List PRs opened by a GitHub App such as Dependabot") is more descriptive than the bare `$ gh issue list --app dependabot` line added to `issue list`. A one-line comment on the issue example would make intent equally clear.
- **Skill prose nit.** The line `is treated as a single keyword (parsed as \`repo:"cli/cli is:open"\`)` runs long and mixes the example with the explanation; splitting the "works / fails" contrast into two clearly-labeled lines would read more cleanly.

### Confidence: High
> The entire change is visible in the diff and is limited to help text, examples, and Markdown; the only residual uncertainty is whether the factual search-syntax claims in `SKILL.md` exactly match runtime behavior.

---
<sub>🤖 Generated by <code>claude-review</code> (claude-builders-bounty #4).</sub>
