<!-- generated by claude-review -->
# 🤖 Claude Code Review — [cli/cli#13599](https://github.com/cli/cli/pull/13599)
> _Alias `gh extension uninstall` to `gh extension remove`_ · by @BagToad · 1 files (+4 / -3)

## 📋 Summary
This PR registers `uninstall` as a Cobra alias for the existing `gh extension remove` subcommand in `pkg/cmd/extension/command.go`. The goal is discoverability: `install`/`uninstall` is a more natural pairing than `install`/`remove`, and aliasing avoids a breaking rename of the canonical `remove` command.

## ⚠️ Risks
- **Alias collision is unverifiable from the diff.** The change adds `Aliases: []string{"uninstall"}` to the `remove` command, but the diff doesn't show the sibling subcommands registered on the same parent (`install`, `list`, etc.). If any sibling already uses `uninstall` as a name or alias, Cobra will resolve ambiguously/unexpectedly. Based on the issue context (#13598) this is almost certainly fine, but it can't be *confirmed* from the four lines shown.
- **No regression risk to `remove` itself.** The canonical `Use`, `Short`, `Args`, and `RunE` are unchanged (only reformatted for gofmt alignment), so existing behavior and the acceptance txtar remain valid. No functional risk identified here.

## 💡 Suggestions
- Consider a small unit/integration test asserting that invoking `gh extension uninstall <name>` routes to the same `RunE`/`m.Remove` path as `remove`. The PR's rationale ("one declarative field, no branching") is reasonable, but an alias is exactly the kind of declarative wiring that silently breaks if a future refactor moves the command — a one-line test pins the contract cheaply.
- The PR description notes docs and the acceptance txtar are intentionally untouched. Worth double-checking that user-facing documentation (e.g. any generated command reference or man pages) doesn't need regeneration, since Cobra surfaces the alias in `--help` — though this is outside the diff and may be auto-generated.
- Optional: mention in the command's `Long`/`Short` help or release notes that `uninstall` is an alias, so users searching docs (not just `--help`) find the pairing.

## 🎯 Confidence
**High** — the change is a single, self-contained, additive Cobra field with no branching logic, and the only residual unknown (alias collision with sibling subcommands) is not visible in the provided diff.
